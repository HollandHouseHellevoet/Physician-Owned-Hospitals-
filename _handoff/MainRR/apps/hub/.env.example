# Hub-specific environment variables.
# Copy to .env.local; never commit secrets.

# Optional: command-palette API endpoint (when wired)
# NEXT_PUBLIC_CMDK_INDEX_URL=https://search.rojasreport.com/index.json
